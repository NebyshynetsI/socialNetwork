import style from './Login.module.css'

const Login = (props) =>{
    return(
        <div className={style.loginForm}>
            Here must be a login form
        </div>
    )
}

export default Login;